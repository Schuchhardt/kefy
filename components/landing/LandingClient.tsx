'use client';

// LandingClient is no longer used — page.tsx renders sections as Server Components directly.
// Kept as empty module to avoid breaking imports if any remain.
export default function LandingClient() {
  return null;
}

